export default function GameObject() {
  /**
   * Init function
   */
  this.init   = () => {};

  /**
   * Update state of the object
   */
  this.update = () => {};

  /**
   * Render the object on the canvas
   */
  this.render = (context) => {};
}